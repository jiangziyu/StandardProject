<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jiangziyu.standard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeWhiteStatus">
        <activity android:name="com.jiangziyu.standard.ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ScrollEventAct"
            android:exported="true">
            <intent-filter>
                <action android:name="com.standard.app.ACTION_START"/>
                <category android:name="com.standard.app.MY_CATEGORY"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.PointDrawAct"/>
    </application>

</manifest>